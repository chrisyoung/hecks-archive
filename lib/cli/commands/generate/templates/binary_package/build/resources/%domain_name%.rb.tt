require 'hecks'
require '<%= domain_name %>'

app = Hecks::Adapters::Application.new(domain: <%= domain_name.camelcase %>)

# Ruby 2.2.2 doesn't support Fixnum#positive?, so monkey patch it.
class Fixnum
  def positive?
    self > 0
  end
end

puts app[ARGV[0].to_sym].create(args).call.inspect
